apiVersion: v1
kind: Service
metadata:
  name: game-portal
  labels:
    app: game-portal
spec:
  type: NodePort
  selector:
    app: game-portal
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30060
      protocol: TCP
